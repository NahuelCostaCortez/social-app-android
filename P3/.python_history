import socket
s = socket.socket()
s.connect((”localhost”, 23))
respuesta = s.recv(1024)
import socket
s = socket.socket()
s.connect((”localhost”, 23))
respuesta = s.recv(1024)
import socket
s = socket.socket()
s.connect(("localhost", 23))
respuesta = s.recv(1024)
respuesta
for byte in respuesta: hex()
for byte in respuesta:
hex()
def mostrar_hex(respuesta):
for byte in respuesta:
def mostrar_Hex(respuesta):
	for byte in respuesta:
		print(hex(byte))
mostrar_Hex(respuesta)
exit
